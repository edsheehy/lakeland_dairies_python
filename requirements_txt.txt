# Lakeland Dairies Batch Processing System Requirements
# Python 3.8+ required

# Modbus communication
pymodbus>=3.5.0

# HTTP client (built-in urllib is used, but requests could be alternative)
# requests>=2.31.0  # Optional alternative to urllib

# Logging and configuration
# All built-in modules used for basic functionality

# Development and testing dependencies (optional)
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1

# Code quality tools (optional)
black>=23.7.0
flake8>=6.0.0
mypy>=1.5.0

# Documentation (optional)
sphinx>=7.1.0
sphinx-rtd-theme>=1.3.0

# Service management (optional, for systemd integration)
python-systemd>=234  # Only on Linux systems with systemd

# Development utilities (optional)
pre-commit>=3.3.0
setuptools>=68.0.0
wheel>=0.41.0
